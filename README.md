# LLM-Chat

This is a vibe-coded project to allow anyone to interact directly with an AI model, hosted either by OpenAI, Claude or any random model from huggingface hosting on Ollama. The idea is a system administrator will create a system prompt, presumably with a secret code, and attempt to prompt inject into it.

The service is running to be hosted via flask on Python.
